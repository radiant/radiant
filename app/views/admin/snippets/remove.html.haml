%h1 Remove Snippet

%p 
  = t('views.snippets.remove_warning_text')

%table.index#snippets{:cellpadding=>0, :cellspacing=>0, :border=>0}
  %tbody
    %tr.node.level-1
      %td.snippet
        = image('snippet', :alt => "snippet-icon")
        %span.title= @snippet.name

- form_for [:admin, @snippet], :html => {:method => :delete} do
  %p.buttons
    %input.button{:type=>"submit", :value => t('views.snippets.delete_snippet')}/
    = t('views.shared.or')
    = link_to t('views.shared.cancel'), admin_snippets_url