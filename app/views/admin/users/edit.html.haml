- @page_title = @user.name + ' Account - ' + default_page_title

- body_classes << "reversed"

- render_region :main do |main|
  - main.edit_header do
    %h1 
      = t('views.users.edit_user')
  - main.edit_form do
    = render :partial => 'form'
