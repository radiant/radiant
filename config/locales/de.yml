de:  
  # The display name of this language file
  this_file_language: "Deutsch" 

  # Controllers       
  resource_controller:
    saved: "{{humanized_model_name}} gespeichert."
    validation_errors: "Bei der Bearbeitung dieses Formulars sind Bestätigungsfehler aufgetreten. Bitte überprüfen und korrigieren Sie alle Eingaben."
    removed: "{{humanized_model_name}} wurde gelöscht."  
    update_conflict: "Seit dem letzten Laden wurde {{humanized_model_name}} geändert. Änderungen können nicht ohne Verlust gespeichert werden." 
  pages_controller:
    saved: "Ihre Seite wurde gespeichert."  
    removed_many: "Die Seiten wurden erfolgreich von der Website gelöscht."   
    removed_one: "Die Seite wurde erfolgreich von der Website gelöscht."  
  preferences_contoller:
    updated: 'Ihre Einstellungen wurden aktualisiert.' 
    error_updating: 'Beim aktualisieren Ihrer Einstellungen ist ein Fehler aufgetreten.'    
  users_controller:
    cannot_delete_self: 'Sie können sich nicht selbst löschen.'   
  welcome_controller:
    logged_out: 'Sie sind jetzt ausgeloggt.'  
    invalid_user: 'Ungültiger Benutzername oder Passwort.'   

  # Models
  models:
    # Shared translations
    required: 'obligatorisch'
    name_in_use: 'Name schon in Benutzung'
    slug_in_use: 'Slug schon in Benutzung als Kind der Elternseite'
    character_limit: '{{count}}-Zeichnen-Limit'
    character_minimum: '{{count}}-Zeichen-Minimum'
    must_be_number: 'Muss eine Zahl sein'
    invalid_format: 'ungültiges Format'
    no_spaces_tabs: 'Darf keine Leerschläge oder Tabs enthalten.'
    # User
    login_in_use: 'Login schon in Benutzung'
    password_confirmation: 'muss der Bestätigung entsprechen'
    invalid_email: 'ungülitge E-Mail-Adresse'  
  
  # Views
  views:   
    shared:
      modify: 'Bearbeiten' 
      more: 'Mehr'
      less: 'Weniger' 
      body: 'Body'  
      name: 'Name'     
      filter: 'Filter'
      cancel: 'Zurück'
      or: 'oder'
    extensions:        
      extensions: 'Erweiterungen' 
      extension: 'Erweiterung'
      website: 'Website'
      version: 'Version'
    # This is the rails application layout  
    layout:
      made_with_ruby: 'Dieses Website ist mit Ruby und Radiant gebaut. '  
      version: 'Version'  
      users: 'Benutzer'
      extensions: 'Erweiterungen' 
      preferences: 'Präferenzen' 
      view_site: 'Website anschauen'
      log_out: 'Ausloggen'
    # These are the templates for the Layouts Controller
    layouts: 
      layouts: 'Layouts'
      layout: 'Layout'  
      description: 'Benutzten Sie Layouts um das Aussehen der Website zu bestimmen.               Layouts können spezielle Tags mit Text oder anderen Elementen wie Header oder Footer     enthalten. Klicken Sie auf einen Layoutnamen, um ihm zu bearbeiten oder <code>Entfernen</code> zu löschen.'
      new_layout: 'Neues Layout'    
      edit_layout: 'Layout bearbeiten'
      content_type: 'Inhalt&#8209;Typ'    
      remove_layout: 'Layout löschen' 
      delete_layout: 'Layout löschen'  
      remove_warning_text: 'Sie sind sicher, das folgende Layout <strong class="warning">dauerhaft</strong> entfernen zu wollen?'   
    page_parts:
      reference:  'Referenz'
      available_tags: 'Available Tags'
    pages:
      pages: 'Seiten'     
      page: 'Seite'     
      new_page: 'Neue Seite'
      edit_page:  'Seite bearbeiten'   
      page_title: 'Titel'  
      remove_tab: 'entfernt einen Tab'  
      add_tab: 'fügt Tab ein'   
      status: 'Status'
      page_type: 'Seitentyp'
      published_at: 'Veröffentlicht am' 
      close: 'Schliessen'
      no_pages: 'keine Seiten' 
      delete_pages: 'lösche {{pages}}'  
      remove_pages: 'entferne {{pages}}' 
      remove_warning_text: 'Sie sind sicher, das Folgende {{pages}} <strong class="warning">dauerhaft</strong> entfernen zu wollen?' 
      available_tags_for: 'Available Tags for'
      search_tags: 'Search Tags:'
      add_part: 'Add Part'
    snippets:
      snippets: 'Snippets'          
      snippet: 'Snippet'   
      new_snippet: 'Neues Snippet'
      edit_snippet: 'Snippet bearbeiten'
      description: 'Snippets sind kleine Stücke Inhalt, die in Seiten oder Layouts eingefügt werden können.'
      remove_warning_text: 'Sind Sie sich sicher, dass Sie das folgende Snippet <strong class="warning">dauerhaft löschen</strong> wollen?'   
      delete_snippet: 'Snippet löschen'
    users:
      user: 'Benutzer'
      users: 'Benutzer'      
      username: 'Benutzername'
      password: 'Passwort' 
      name: 'Name'
      login: 'Login' 
      edit_user: 'Benutzer bearbeiten'
      new_user: 'Neuer Benutzer'  
      roles: 'Rollen'
      admin: 'Administrator' 
      developer: 'Entwickler'
      language: 'Sprache'
      notes: 'Notiz'    
      optional: 'Optional'
      required: 'erforderlich'  
      valid_email: 'Bitte benutzen Sie eine gültige E-Mail-Adresse.' 
      valid_login: 'Minimum 3 Zeichnen. Muss eindeutig sein. '
      valid_password: 'Mindestens 5 Zeichen.'
      leave_password_unchanged: 'Lassen Sie das Passwortfeld leer, wenn Sie Ihr Passwort nicht ändern wollen.'
      password_confirmation: 'Passwort bestätigen'
      roles_description: 'Rollen beschränken die Möglichkeiten der Benutzer und schalten Teile des Administrationsinterfaces an oder aus.' 
      language_description: 'Die bevorzugte Sprache für das Admin-Interface.' 
      remove_warning_text: 'Sind Sie sicher, dass Sie den folgenden Benutzer <strong class="warning">dauerhaft löschen</strong> wollen?' 
      delete_user: 'Benutzer löschen'
    preferences:
      user_preferences: 'Benutzer Einstellungen'
    welcome:
      login: 'Bitte Anmelden'
      login_btn: 'Login'
      remember_me: 'Merken'

       
  
  # Page status  
  status:
    draft: 'Entwurf'
    reviewed: 'Überprüft'
    hidden: 'Versteckt'
    published: 'Publiziert' 
  
  # Buttons
  buttons:
    save_and_continue: 'Sichern und bearbeiten'
    save_changes: 'Änderungen sichern'  
    create: '{{name}} erstellen'     
    
  # Timestamp. This may need to be changed as it will most likely not work for many languages.
  timestamp:
    last_updated: 'Zuletzt geändert'
    by: 'von'
    at: 'um'
    
  # Select lists
  select:
    none: '<keine>' 
    normal: '<normal>'
    inherit: '<vererbt>'
    
  # Main Navigation Tags
  tabs: 
    pages: 'Seiten'
    snippets: 'Snippets'
    layouts: 'Layouts'
    
  # Standard Tag Descriptions
  tag_desc:
    usage: 'Benutzung'
    produces: 'produces'

    page:
      desc: "Causes the tags referring to a page's attributes to refer to the current page."
      attributes: 'Renders the @{{method}}@ attribute of the current page.'
      url: 'Gibt das @url@ Attribut der aktuellen Seite aus.'

    children: 
      desc: "Ermöglicht Zugang zu den Unterseiten einer Seite."
      count: 'Gibt die Gesamtanzahl der Unterseiten aus.'
      first: 'Liefert die erste Unterseite zurück. Innerhalb dieses Tags werden alle Tags der Seiten-Attribute auf die erste Unterseite bezogen. <br /> 
        Akzeptiert die selben Sortieroptionen, wie @<r:children:each>@.'    
      last: 'Liefert die letzte Unterseite zurück. Innerhalb dieses Tags werden alle Tags der Seiten-Attribute auf die letzte Unterseite bezogen. <br /> 
        Akzeptiert die selben Sortieroptionen, wie @<r:children:each>@.›'
      each: "Durchläuft jede der Unterseiten. Innerhalb dieses Tags werden alle Tags der Seiten-Attribute auf die aktuelle Unterseite bezogen."
      child: "Seitenattribut-Tags innerhalb dieses Tags beziehen sich auf die aktuelle Unterseite. Dies ist insbesondere <br /> 
        wenn Sie in einem anderen Tag (wie &lt;r:find&gt;) sind und zurück zur aktuellen Unterseite<br /> referenzieren müssen."  
      if_first: "Gibt den Inhalt nur aus, wenn die aktuelle Unterseite die erste in einem Tag wie children:each ist."
      unless_first: "Gibt den Inhalt nur dann aus, wenn die aktuelle Unterseite in einem children:each-Tag nicht die erste Unterseite ist."
      if_last: "Gibt den Inhalt nur aus, wenn die aktuelle Unterseite die letzte in einem Tag wie children:each ist."
      unless_last: "Gibt den Inhalt nur dann aus, wenn die aktuelle Unterseite in einem children:each-Tag nicht die letzte Unterseite ist."
      header:
        part_1: "Gibt den Inhalt nur aus, wenn der Inhalt nicht dem vorhergehenden Header entspricht. Dies ist insbesondere hilfreich um Datenheader in einer Liste von Unterseiten ausgegeben werden soll."
        part_2: "Wenn Sie mehrere Header-Blöcke ausgeben wollen, können Sie das @name@-Attribut benutzen um dem Header einen Namen zu geben. When a header is named it will not restart until another header of the same name is different."
        part_3: "Using the @restart@ attribute you can cause other named headers to restart when the present header changes. Simply specify the names of the other headers in a semicolon separated list."

    parent: "Page attribute tags inside this tag refer to the parent of the current page. "
    if_parent: "Renders the contained elements only if the current contextual page has a parent, i.e. is not the root page."
    unless_parent: "Renders the contained elements only if the current contextual page has no parent, i.e. is the root page."

    if_children: 'Renders the contained elements only if the current contextual page has one or <br />
      more child pages.  The @status@ attribute limits the status of found child pages <br />
      to the given status, the default is @"published"@. @status="all"@ includes all <br />
      non-virtual pages regardless of status.'
    unless_children: 'Renders the contained elements only if the current contextual page has no children.
      The @status@ attribute limits the status of found child pages to the given status,
      the default is @"published"@. @status="all"@ includes all non-virtual pages
      regardless of status.'

    cycle: 'Renders one of the passed values based on a global cycle counter.  Use the @reset@
      attribute to reset the cycle to the beginning.  Use the @name@ attribute to track
      multiple cycles; the default is @cycle@.'
    content: "Renders the main content of a page. Use the @part@ attribute to select a specific
      page part. By default the @part@ attribute is set to body. Use the @inherit@
      attribute to specify that if a page does not have a content part by that name that
      the tag should render the parent's content part. By default @inherit@ is set to
      @false@. Use the @contextual@ attribute to force a part inherited from a parent
      part to be evaluated in the context of the child page. By default 'contextual'
      is set to true."

    if_content:
      part_1: 'Renders the containing elements if all of the listed parts exist on a page.
        By default the @part@ attribute is set to @body@, but you may list more than one
        part by separating them with a comma. Setting the optional @inherit@ to true will
        search ancestors independently for each part. By default @inherit@ is set to @false@.'
      part_2: 'When listing more than one part, you may optionally set the @find@ attribute to @any@
        so that it will render the containing elements if any of the listed parts are found.
        By default the @find@ attribute is set to @all@.'
    unless_content:
      part_1: 'The opposite of the @if_content@ tag. It renders the contained elements if all of the
        specified parts do not exist. Setting the optional @inherit@ to true will search
        ancestors independently for each part. By default @inherit@ is set to @false@.'
      part_2: 'When listing more than one part, you may optionally set the @find@ attribute to @any@
        so that it will not render the containing elements if any of the listed parts are found.
        By default the @find@ attribute is set to @all@.'

    if_url: "Renders the containing elements only if the page's url matches the regular expression
      given in the @matches@ attribute. If the @ignore_case@ attribute is set to false, the
      match is case sensitive. By default, @ignore_case@ is set to true."
    unless_url: "The opposite of the @if_url@ tag."

    if_ancestor_or_self:
      part_1: "Renders the contained elements if the current contextual page is either the actual page or one of its parents."
      part_2: "This is typically used inside another tag (like &lt;r:children:each&gt;) to add conditional mark-up if the child element is or descends from the current page."
    unless_ancestor_or_self:
      part_1: "Renders the contained elements unless the current contextual page is either the actual page or one of its parents."
      part_2: "This is typically used inside another tag (like &lt;r:children:each&gt;) to add conditional mark-up unless   the child element is or descends from the current page."
    if_self:
      part_1: "Renders the contained elements if the current contextual page is also the actual page."
      part_2: "This is typically used inside another tag (like &lt;r:children:each&gt;) to add conditional mark-up if the child element is the current page."
    unless_self: 
      part_1: "Renders the contained elements unless the current contextual page is also the actual page."
      part_2: "This is typically used inside another tag (like &lt;r:children:each&gt;) to add conditional mark-up unless the child element is the current page."

    author: 'Renders the name of the author of the current page.'

    date: "Renders the date based on the current page (by default when it was published or created).
      The format attribute uses the same formating codes used by the Ruby @strftime@ function. By
      default it's set to &#37;A, &#37;B &#37;d, &#37;Y. The @for@ attribute selects which date to render.  Valid
      options are @published_at@, @created_at@, @updated_at@, and @now@. @now@ will render the
      current date/time, regardless of the  page."

    link: "Fügt einen Link in die Seite ein. Wenn Sie ihn als einzelnen Tag benutzen, wird der Seitentitel als Linktext benutzt. Wird er als doppelter Tag benutzt, wird der Text zwischen Anfangs- und Schlusstag als Linktext benutzt. Der Link-Tag übergibt alle Attribute an den HTML
      @a@-Tag. Das ist nützlich um das @class@ oder @id@ Attribut zu übergeben. Wenn das @anchor@ Attribut übergeben wird, wird ein Rautenzeichen (<code>#</code>) gefolgt von dem Namen in das @href@ attribute des HTML @a@ Tags eingefügt --so wird ein HTML Anker erstellt."

    breadcrumbs: "Renders a trail of breadcrumbs to the current page. The separator attribute
      specifies the HTML fragment that is inserted between each of the breadcrumbs. By
      default it is set to @>@. The boolean nolinks attribute can be specified to render
      breadcrumbs in plain text, without any links (useful when generating title tag)."
    snippet: 
      part_1: "Renders the snippet specified in the @name@ attribute within the context of a page."
      part_2: "When used as a double tag, the part in between both tags may be used within the snippet itself, being substituted in place of @<r:yield/>@."

    yield:
      part_1: "Used within a snippet as a placeholder for substitution of child content, when the snippet is called as a double tag."
      usage: "Usage (within a snippet):"
      part_2: 'If the above snippet was named "yielding", you could call it from any Page,
        Layout or Snippet as follows:'
      part_3: 'Which would output the following:'
      part_4: "When called in the context of a Page or a Layout, @<r:yield/>@ outputs nothing."

    meta:
      desc: "The namespace for 'meta' attributes. If used as a singleton tag, both the description and keywords fields will be output as &lt;meta /&gt; tags unless the attribute 'tag' is set to 'false'."
      keywords: "Emits the page keywords field in a meta tag, unless attribute 'tag' is set to 'false'."
      description: "Emits the page description field in a meta tag, unless attribute 'tag' is set to 'false'."

    status:
      desc: "Prints the page's status as a string. Optional attribute 'downcase' will cause the status to be all lowercase."

    unless_dev:
      desc: "The opposite of the @if_dev@ tag."

    if_dev:
      desc: "Renders the containing elements only if Radiant in is development mode."

    navigation:
      desc: "Renders a list of links specified in the @urls@ attribute according to three states: <br/>
        * @normal@ specifies the normal state for the link<br/>
        * @here@ specifies the state of the link when the url matches the current page's URL<br/>
        * @selected@ specifies the state of the link when the current page matches is a child of the specified url<br/>
        The @between@ tag specifies what should be inserted in between each of the links."
      error: "`navigation' tag must include a `normal' tag"

    rfc1123_date:
      desc: "Outputs the published date using the format mandated by RFC 1123. (Ideal for RSS feeds.)"

    escape_html:
      desc: "Escapes angle brackets, etc. for rendering in an HTML document."

    comment:
      desc: "Dies ist ein Kommentar daher erfolgt keine Ausgabe."

    random:
      desc: "Liefert zufällig einen Wert aus den @option@ Tags zurück."

    find:
      desc: "Innerhalb dieses Tags beziehen sich alle zur Seite gehörenden Tags auf die Seite, die durch das @url@-Attribute gefunden werden. @url@s können relative oder absolute Pfade sein."

    markdown:
      desc: "Filters its contents with the Markdown filter."
    smarty_pants:
      desc: "Filters its contents with the SmartyPants filter."
    textile:
      desc: "Filters its contents with the Textile filter."


  # Date and Time formats  
  date:
    formats:
      default: "%d.%m.%Y"
      short: "%e. %b"
      long: "%e. %B %Y"

    day_names: [Sonntag, Montag, Dienstag, Mittwoch, Donnerstag, Freitag, Samstag]
    abbr_day_names: [So, Mo, Di, Mi, Do, Fr, Sa]
    month_names: [~, Januar, Februar, März, April, Mai, Juni, Juli, August, September, Oktober, November, Dezember]
    abbr_month_names: [~, Jan, Feb, Mär, Apr, Mai, Jun, Jul, Aug, Sep, Okt, Nov, Dez]
    order: [:day, :month, :year]

  time:
    formats:
      timestamp: "%H:%M am %d. %B, %Y"
      # Radiant doesn't really use these, but let's leave them here just in case.
      default: "%A, %e. %B %Y, %H:%M Uhr" 
      time: "%H:%M Uhr"
      short: "%e.%m., %H:%M Uhr"
      long: "%A, %e. %B %Y, %H:%M Uhr"

    am: ""
    pm: ""